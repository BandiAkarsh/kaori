# Edge OS — Calamares Initramfs Module
#
# Debian-specific module: rebuilds initramfs post-installation
# to ensure new kernel and fs modules are included.

---
# Kernel version to target: "all" rebuilds for all installed kernels
kernel: "all"

# Security: don't turn off permission checks on initramfs
be_unsafe: false
