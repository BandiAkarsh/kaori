# Edge OS — Calamares Network Configuration Module
#
# Copies network configuration from the live environment to the target system.
# This preserves wifi/network settings across installation.

---
# Empty config — module uses default behavior:
# Copies /etc/NetworkManager/system-connections/ from live to target
